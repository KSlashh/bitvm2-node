\* Full system: ChainScanNext (Bitcoin-poll) racing against GoatRaceNext
\* (GoatChain L2-event-watcher), matching the real code where both are
\* independently-scheduled tasks writing the same `graph.status` column
\* with no coordination. Expected result: NoConflictingWithdrawal,
\* TerminalStatusesAreAbsorbing and DisproveIsSticky FAIL - that failure
\* is the bug this spec exists to demonstrate, not a spec error.
SPECIFICATION FairFullSpec

CHECK_DEADLOCK FALSE

INVARIANT TypeOK

PROPERTY NoConflictingWithdrawal
PROPERTY DisproveIsSticky
PROPERTY TerminalStatusesAreAbsorbing
